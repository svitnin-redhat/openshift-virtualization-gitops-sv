apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../common
  - clusterinstance.yaml

namespace: ocp1m0w-b

patches:
- patch: |-
    - op: replace
      path: /metadata/name
      value: ocp1m0w-b
  target:
    kind: Namespace
    name: CLUSTER_ID

- patch: |-
    - op: replace
      path: /metadata/name
      value: ocp1m0w-b-cr
  target:
    kind: ClusterRoleBinding
    name: CLUSTER_ID-cr
